<% content_for(:breadcrumb) do
  render 'admin/partials/breadcrumb', page_name: 'Sergi Resimleri', icon:'list'
end %>
<div class="row">

    <% @admin_exhibition_works.each do |exhibition_work| %>

      <div class="col-lg-3 text-center">
        <div class="panel panel-default">
          <div class="panel-body">
            <%= image_tag exhibition_work.photo.url(:thumb), width: '225' %>
            <div class="panel-footer">
              <%= link_to 'Düzenle', edit_admin_exhibition_photo_path(@exhibition, exhibition_work),   :class => "btn btn-xs btn-danger"%>
              <%= link_to 'Sil', admin_exhibition_photo_path(@exhibition, exhibition_work), method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-xs btn-success"  %>
            </div>
          </div>
        </div>
      </div>

    <% end %>

</div>



<%= link_to new_admin_exhibition_photo_path(@exhibition), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
    Yeni Resim
<% end %>

